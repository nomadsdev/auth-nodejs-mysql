<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include('./Config/Import') %>
</head>
<body>

    <div class="flex justify-center px-5">
        <div class="w-full max-w-7xl">
            <div>

                <div class="flex justify-center pt-10">
                    <h1 class="text-centet font-bold text-4xl">
                        Authentication <span class="text-green-500">Nodejs</span> <span class="text-blue-500">Mysql</span>
                    </h1>
                </div>
                <br>
                <% if (!LogIn) { %>
                    <div class="flex justify-center">
                        <div class="w-full max-w-xl flex justify-center gap-5">
                            <a href="/login" class="bg-blue-500 text-white rounded-xl border border-blue-500 hover:bg-white transition hover:text-blue-500 duration-300 px-10 py-2">
                                Sing In
                            </a>
                            <a href="/register" class="bg-blue-500 text-white rounded-xl border border-blue-500 hover:bg-white transition hover:text-blue-500 duration-300 px-10 py-2">
                                Sing Up
                            </a>
                        </div>
                    </div>
                <% } %>
                <% if (LogIn) { %>
                    <div>
                        <p class="text-center">
                            Username: <%= username %>
                        </p>
                        <br>
                        <div class="flex justify-center">
                            <a href="/api/auth/logout" class="flex items-center justify-center gap-1 text-xs px-10 py-2 text-white bg-red-500 rounded-xl border border-red-500 transition hover:bg-white hover:text-red-500 duration-300">
                                <span class="flex">
                                    <i class="ph-duotone ph-sign-out"></i>
                                </span>
                                Logout
                            </a>
                        </div>    
                    </div>
                <% } %>

            </div>
        </div>
    </div>
    
</body>
</html>